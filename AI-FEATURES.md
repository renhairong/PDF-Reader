# AI 功能需求细化（讨论稿）

> 基于云轩 PDF 阅读器底座（本地优先、纯前端、批注系统、IndexedDB）
> 状态：需求讨论阶段，决策点待确认

---

## 功能总览

| 功能 | 核心价值 | 依赖 |
|---|---|---|
| 1. 梳理核心内容 | PDF → 可读的章节化要点笔记 | LLM |
| 2. 生成思维导图 | 笔记 → 可视化树状结构 | LLM + 导图渲染 |
| 3. 生成播客 | 内容 → 对话式音频（"听书"）| LLM + TTS |

---

## 功能一：梳理核心内容

### 输入范围（多选）
- [ ] 整本 PDF
- [ ] 当前章节 / 当前页
- [ ] 选中文字（选区触发）
- [ ] 结合用户批注（IndexedDB 中已有批注 → 作为"我关注的重点"输入）

### 输出形式
- 章节化笔记：`标题层级 + 要点列表`
- 每章 3-6 个要点，要点带**页码锚点**（可跳回原文）
- 总览摘要（全文 200-400 字）

### 交互
- 一键生成（整本）→ 生成后可**追问式对话**（"第三部分讲了什么""作者对 X 的观点是"）
- 进度反馈：长文分块处理，显示进度（参考打印进度条方案）

### 关键点
- 长 PDF 超上下文：需**分块 + 分层摘要**（每章摘要 → 汇总成总摘要）
- 页码锚点：利用 pdf.js 每页文本定位

---

## 功能二：生成思维导图

### 数据来源
- 基于功能一的梳理结果生成（推荐，结构一致）
- 或直接读 PDF 生成

### 结构
- 中心主题 → 一级分支（章）→ 二级（节）→ 三级（要点）
- 默认 3-4 层，超长自动裁剪

### 输出形式
- [ ] 网页交互式（svg/canvas，可展开折叠、拖拽）
- [ ] 导出 PNG 图片
- [ ] 导出 Markdown（可导入 XMind 等工具）

### 可编辑
- 生成后手动增删/改节点文字（存 IndexedDB，和批注体系一致）

---

## 功能三：生成播客

### 形式
- [ ] 单人讲解
- [ ] **双人对话**（AI 主持 + 嘉宾，讨论风格，听感更好）— 参考 Gemini Podcast
- [ ] 都要

### 输出
- 脚本（文字稿，可编辑后重新生成音频）
- 音频文件（MP3/WAV）

### TTS 方案
| 方案 | 质量 | 成本 | 说明 |
|---|---|---|---|
| 系统 TTS（macOS say / edge-tts）| 中 | 免费 | 本地，中文自然度一般 |
| 云端 TTS（火山/阿里/OpenAI）| 高 | 按量付费 | 需要 key，效果好 |

### 时长
- 短版：3-5 分钟（单章节/选中内容）
- 长版：15-30 分钟（整本）
- 可配"对话密度"（详略）

---

## 关键架构决策（待拍板）

| # | 决策点 | 选项 | 建议 |
|---|---|---|---|
| 1 | **LLM 来源** | A 云端 API（需 key）B 本地模型（Ollama）C 云端+本地预留 | C：先云端跑通，预留本地接口 |
| 2 | **开发顺序** | 梳理 → 导图 → 播客 | 先梳理（验证链路），再导图，最后播客 |
| 3 | **与阅读器打通** | A 笔记带页码跳转 B 独立面板 | A：和批注/阅读联动，价值更高 |
| 4 | **后端形态** | A 纯前端（LLM 直连，key 放本地）B 轻量本地代理（Node/Python，key 不暴露）| B 更稳（避免 CORS/key 暴露），但增加部署 |
| 5 | **费用归属** | 谁承担 token/TTS 费用 | 用户自备 key，按量计费 |
| 6 | **多语言** | 中文为主 / 中英混合 | 中文优先，英文 PDF 也支持输出中文 |

---

## 用户确认后的下一步

1. 定 LLM 来源（准备 key / Ollama 地址）
2. 定开发顺序
3. 写 TASKS.md 拆解任务
